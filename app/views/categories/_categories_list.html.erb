<!-- Pass the categories object to this partial -->
<% sorted_categories = categories.sort_by{ |category| category.name.downcase } %>

<% sorted_categories.each do |category| %>
  <%= link_to "<#{category.name}>", category_path(category), class: "btn btn-default btn-xs" %>
<% end %>

